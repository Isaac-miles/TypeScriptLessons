import MuiModal from '@mui/material/Modal'

function Modal() {
    const handleClose =()=>{
        // set modal to false
    }
  return (
    <MuiModal open={true} onClose={handleClose}>
    <>
    
    </>
    </MuiModal>

  )
}

export default Modal